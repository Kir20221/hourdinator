{{ form_start(form) }}
    {{ form_widget(form.nom) }}
    {{ form_widget(form.adresse.ligne1) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_widget(form) }}
{{ form_end(form) }}
