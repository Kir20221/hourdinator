{% extends 'base.html.twig' %}

{% block title %}Article index{% endblock %}

{% block body %}
    <h1>Article index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Designation</th>
                <th>Prix</th>
                <th>Catégorie</th>
                <th>Achetable par</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td>{{ article.id }}</td>
                <td>{{ article.code }}</td>
                <td>{{ article.designation }}</td>
                <td>{{ article.prix }}</td>
                <td>{{ article.categorie.code }} {{ article.categorie.nom }}</td>
                <td>
                    {% for categ in article.achetablePar %}
                        <div>{{ categ.code }} {{ categ.nom }}</div>
                    {% endfor %}
                <td>
                    <a href="{{ path('app_article_show', {'id': article.id}) }}">show</a>
                    <a href="{{ path('app_article_edit', {'id': article.id}) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_article_new') }}">Create new</a>
{% endblock %}
