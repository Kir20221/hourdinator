{% extends 'base.html.twig' %}

{% block title %}CommandeStatut{% endblock %}

{% block body %}
    <h1>CommandeStatut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_statut.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ commande_statut.designation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commande_statut_index') }}">back to list</a>

    <a href="{{ path('app_commande_statut_edit', {'id': commande_statut.id}) }}">edit</a>

    {{ include('commande_statut/_delete_form.html.twig') }}
{% endblock %}
