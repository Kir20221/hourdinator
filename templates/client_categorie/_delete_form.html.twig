<form method="post" action="{{ path('app_client_categorie_delete', {'id': client_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_categorie.id) }}">
    <button class="btn">Supprimer</button>
</form>
